---
interface Props {
  /** If true (default), each direct child is revealed one-by-one. If false, the whole block reveals at once. */
  items?: boolean;
}

const { items = true } = Astro.props;
---

<div data-reveal={items ? 'items' : 'block'}>
  <slot />
</div>
