# Review: 03-graph-mode.mdx (2026-09-05)

리뷰어: Claude Fable 5.1 (서브에이전트) + OpenAI Codex `gpt-6-astra`.
대상: `src/content/lectures/03-graph-mode.mdx` (733행). 읽기 전용 리뷰, 수정 없음.

## TL;DR

- **AI slop / 용어**: 대체로 깨끗함. 음차("컨트롤 플로우 그래프", "상징 실행", "커널 퓨전")와 표기 불일치(FxGraph/FX Graph, Super-scalar) 정도. 비표준 용어: "Trace Generation / Trace Replay"(Dynamo 문서에 없음), "Backend Object"(655행). 두 리뷰어 일치.
- **이해도**: 가장 큰 문제. Dynamo의 guard → 실행 → graph break → resume 루프가 산문 2회 + 번호 목록 3회 + Mermaid 1회 + SVG 1회로 **6번 반복**(390–563행). Trace cache 절(190–231)은 Dynamo 강의 대비 과도하게 길고 Pentium 4 구현과 연구 구조를 섞음. FX graph·guard·`fullgraph`·joint graph 정의가 늦거나 없음. 두 리뷰어 일치.
- **다이어그램**: 14개 중 8개는 문제없음. 공통 지적: IR lowering Mermaid(570)의 "Loop-Level IR → Triton" 엣지가 틀림(Triton은 codegen된 kernel 소스를 받음), slide22_1(442)은 판독 불가 + 본문 5단계와 라벨 불일치, 467 Mermaid는 recompile→실행 엣지 없음, trace-generation SVG(510)는 guard 검사가 bytecode 실행 뒤로 읽힘, slide15_1(386)은 CPython ≤3.10 frame 구조.
- **정확성**: 두 리뷰어 공통 high: 380행 "frame 내부에 evaluation loop 존재"(evaluation loop은 frame을 인자로 받는 interpreter 함수), 313행 `jit.script`≈partial evaluator(AOT 컴파일러이며 특화 안 함), 59행 "AOTAutograd가 백엔드에 포함"(frontend), 213행 outstanding trace buffer 역할.
- **Codex만 잡았고 검증 완료된 high 2건**: (1) 323–335행 예제 `x.cos().cos()` 후 `mean() > 0.5` — cos(cos(x)) ∈ [0.54, 1]이라 어떤 입력에도 분기가 바뀌지 않아 "trace 안전성 없음" 예제로 작동하지 않음. (2) 536행 `accumulated_recompile_limit`를 "프로세스 전체"라 했으나 v2.13.0 `cache_size.py`는 "across ALL regions on this code object"로 **code object 단위**. Fable은 이 줄을 "검증 통과"로 표시했으나 틀렸음.
- **Fable만 잡은 high였으나 출처 검증에서 기각**: 699행 Prims "123개"를 Fable은 "≤87개"라 정정했지만, v2.13 공식 IR 문서의 Prims 표는 정확히 123개이고 `torch._prims.__all__`도 127개. 강의 원문이 맞음.

## 두 리뷰어가 일치한 항목 (우선 수정 후보)

| line | 문제 | 제안 |
|---|---|---|
| 59 / 69 | "자동 미분이 백엔드에 포함" | slide03_1대로 AOTAutograd는 frontend(Integration Interface 위) |
| 110, 182, 339 | 음차 "컨트롤 플로우 그래프", "상징 실행", "커널 퓨전" | 영문 표기로 통일, symbolic execution은 361행에서 한 번 정의 |
| 132 | "코드 한 줄이 하나의 노드" | CFG 그림과 불일치. "L1-2는 연속 두 문장, 세 조건은 D 하나, J1은 합류점(basic block)" |
| 150-155 | trace-diagram SVG 두 trace가 색만 다름 | 입력 `d>0`/`d=0`과 경로 노드명 표시 |
| 174 / 192 | "5~6개마다 branch", "bank conflict/cache miss" | 출처 필요. trace cache 동기는 taken branch 넘는 fetch bandwidth |
| 190-231 | Trace cache 절 과다, Pentium 4 vs 연구 구조 혼합 | slide10_2 + 3단계만 남기고 SVG(196)·231행 삭제 |
| 213, 228 | outstanding trace buffer = "유망 후보 선별" | trace 조립 + branch 결과 추적/오예측 복구 버퍼 |
| 261-273 | partial-evaluation SVG ①②③ vs 본문 bullet 2개, ① 위치 | 지시선을 주황 칸에, bullet 3개로 |
| 268 | "static input이 포함된 연산 모두 제거" | "static input에만 의존하는 연산" |
| 300-313 | `jit.script` = Python subset interpreter / partial evaluator | AST를 TorchScript IR로 컴파일, 제어 흐름 보존, 특화 안 함 |
| 301 | "CPython에 직접 통합" | PEP 523 hook. 345행과 모순 |
| 380 | "frame 내부에 evaluation loop" | frame = code ref + locals + value stack. eval loop은 `_PyEval_EvalFrameDefault` |
| 386 | slide15_1 CPython ≤3.10 구조 | "3.10 이전 개념도" 캡션. 3.11+는 `_PyInterpreterFrame` |
| 390-563 | Dynamo 루프 6중 반복 | 390-408 SVG + 4단계, 450-465 cache entry 구성, 467 Mermaid(수정) 하나만. 506-520 삭제, 522-538은 540 정리 절에 흡수 |
| 442-446 | slide22_1 판독 불가, 5단계 명칭·순서가 그림과 불일치 | 그림 크롭 또는 교체, 446행을 그림 라벨 순서로 |
| 467-492 | Mermaid: RECOMPILE→TB 엣지 없음, RF1/RF2 동일 라벨, "PyFrameObject 호출" | 엣지 추가, 라벨 "분기 A/B", "함수 호출 → 새 frame" |
| 504 | "안쪽 함수들까지 모두 eager" | 새 컴파일만 중단, 기존 entry는 guard 통과 시 재사용 |
| 510-515 | trace-generation SVG: guard가 실행 뒤로 읽힘, 8.5px 주석 | 삭제 권장(467과 중복) |
| 538 | `dynamic=True`가 "자동 전환 On" | 첫 컴파일부터 dynamic(비권장), `False`는 항상 specialize |
| 570-591 | "Loop-Level IR → Triton" | Inductor codegen이 Triton kernel 소스 생성 → Triton compiler → PTX |
| 583 | joint graph/partitioning/functionalization/normalization 무정의 | Week 4로 미루고 한 줄 정의만 |
| 651 / 717 | "두 번째부터 compilation overhead 없음" | "guard 통과하는 cache 재사용, 새 조건이면 추가 컴파일" |
| 655-670 | `model(input)` (builtin 가림, `my_input` 아님), `MyRuntime` 미구현 | `my_input`으로. 의사코드 표시 |
| 677-688 | "AOTAutograd가 Core Aten graph 생성" | `decompositions=` 없이는 functional ATen. `core_aten_decompositions()` 인자 추가. needs verification |
| 727-729 | Q "Python에서만?" A "Python으로 작성됨" | 답을 사용 범위(Python 모델에만 적용, export는 별도)로 |

## 한쪽만 제기한 항목 (검토 필요)

- **Codex A4-4 (high, 검증됨) line 323-335**: `cos().cos()` 예제는 분기가 안 바뀜. 임계값 0.8로 바꾸고 `zeros` vs `full(π/2)` 비교.
- **Codex A4-5 (high, 검증됨) line 536**: `accumulated_recompile_limit`는 code object 단위(`total_cache_entries_all_regions`), "프로세스 전체" 아님.
- **Codex A4-3 (high) line 341**: "TorchScript는 trace만 뽑고 최적화는 별도" → TorchScript에도 JIT 최적화 경로 있음.
- **Codex A4-7 (high) line 190-207**: Pentium 4 trace cache miss는 L2 → decode 경로. "별도 L1 I-cache에서 하나씩"이 아님.
- **Codex A4-10 line 353**: "Python 전체 언어 지원" → capture 범위 있음. graph break/skip으로 공존.
- **Codex A4-12 line 548**: "기록이나 최적화 없이" → CPython 3.11+ adaptive specialization 존재.
- **Codex A4-16 line 619**: `stable` 링크는 2.14로 이동함. 2.13 스냅샷 또는 태그 소스로 고정.
- ~~**Fable A4-1 (high) line 699**: Prims 123개 → ≤87개~~ **기각** (출처 검증 #2·#13: 공식 2.13 문서 표 123개, 소스 `__all__` 127개). 2.0 발표 "~250"만 유효.
- **Fable A2-4 line 309/321**: "tensor 연산은 dynamic input" → tensor **값**이 dynamic, 제어 흐름과 그 결정값이 static.
- **Fable A2-6 line 496**: `fullgraph` 최초 등장인데 정의 없음.
- **Fable A4-11 line 583**: "Aten IR 또는 Prims IR" → Inductor 경로는 ATen. Prims는 별도 경로. needs verification.
- **Fable 검증 통과 목록 중 536행 항목은 위와 같이 틀렸으므로 무시.** 나머지(2.10 deprecation, Core Aten 193개, automatic dynamic 2.1, skip 규칙, `aot_autograd`/`make_boxed_func` API)는 유효.

## 출처 검증 (2026-09-05)

검증 방법: `curl -sL`로 `raw.githubusercontent.com/pytorch/pytorch/v2.13.0|v2.10.0|v2.9.0/...` 소스 직접 조회(+`sed -n`/`ast`/`pdftotext`로 정밀 대조), `docs.pytorch.org/docs/{2.9,2.13,2.14,stable,main}/...` 및 `docs.python.org/3.11/...`는 `curl -sL` 후 HTML에서 본문 추출, 논문 2건(Rotenberg 1999, Intel Technology Journal 2001)은 PDF 다운로드 후 `pdftotext -layout`으로 원문 대조. 대상 파일 경로는 실제로 `/home/appleparan/src/pytorch-internal-lecture/.claude/worktrees/docs-review-2026-09-05/docs/reviews/2026-09-05/03-graph-mode.md`(과제에 적힌 경로는 세그먼트 중복으로 존재하지 않음).

**핵심 발견(요약 먼저)**: 병합 요약과 Fable 리포트가 "검증 완료"라 표시한 **699행 Prims 개수 정정("88개/≤87개")은 틀렸다.** v2.13.0 `torch/_prims/__init__.py`를 AST로 직접 파싱하면 `__all__`은 127개(RETURN_TYPE 제외 126개 연산)이고, PyTorch가 실제로 배포한 **2.13 공식 문서(`docs.pytorch.org/docs/2.13/.../torch.compiler_ir.html`)의 Prims IR 표는 정확히 123개**를 나열한다. 즉 강의 원문의 "123개"가 정확하고, Fable의 "정정"이 불일치다.

| # | 인용 위치 | 출처 | 접근 | 판정 | 근거 (인용/줄 번호) |
|---|---|---|---|---|---|
| 1 | 병합 요약 12행 | `torch/_dynamo/cache_size.py` (v2.13.0) | 200 | 지지 | "Total cache entries across ALL regions on this code object. ... a global safety cap." (raw L88-89, L100-102) — accumulated_recompile_limit은 code object 단위 |
| 2 | 병합 요약 13·54행 | `torch/_prims/__init__.py` (v2.13.0) `__all__` | 200 | **불일치** | AST 파싱 결과 `__all__` 127개(비-연산 `RETURN_TYPE` 제외 126개). "88개/≤87개"는 근거 없음. 공식 2.13 문서 표는 123개(행 #… 아래 참조) |
| 3 | Fable Axis3, line 108 (slide10_1) | Rotenberg–Bennett–Smith 1999 논문 Fig. 2 | 200(PDF) | 지지 | "Fig. 2. Microarchitecture." — Trace Predictor·Trace Cache·Execution Engine·outstanding trace buffers·`update`/`branch outcomes` 피드백 화살표 모두 존재(p.113) |
| 4 | Fable Axis4, line 127 (검증 기준 파일 목록) | `torch/_dynamo/{config.py,convert_frame.py,trace_rules.py}`, `torch/csrc/dynamo/eval_frame_cpp.cpp`, `torch/_dynamo/backends/common.py`, `functorch/compile/__init__.py`, `aten/.../native_functions.yaml`, `torch/_prims/__init__.py`, `docs/source/scripts/build_opsets.py`, 5개 docs md, `torch/jit/_trace.py`(v2.9.0/v2.10.0) | 200(전부) | 지지 | 나열된 모든 경로가 v2.13.0(또는 명시된 태그)에 실제 존재(HTTP 200) |
| 5 | Fable Axis4, line 129 (`convert_frame.py:2035-2040`) | `torch/_dynamo/convert_frame.py` (v2.13.0) | 200 | 지지(줄 번호 근사) | "Set frame execution strategy to RUN_ONLY ... `FrameExecStrategy(FrameAction.RUN_ONLY, FrameAction.RUN_ONLY)`" 실제 위치는 L2035-2037(인용 범위 2035-2040과 거의 일치) |
| 6 | Fable Axis4, line 129 (2.10 deprecation) | `torch/jit/_trace.py` v2.9.0 vs v2.10.0 | 200 | 지지 | v2.10.0 L997-1002: "`torch.jit.trace` is deprecated..." `DeprecationWarning`; v2.9.0에는 해당 경고 없음(확인) |
| 7 | Fable Axis4, line 129 (Core Aten 193개) | `aten/src/ATen/native/native_functions.yaml` (v2.13.0) | 200 | 지지 | `tags:.*core` 매칭 193건(grep) — 공식 2.13 문서 Core Aten IR 표의 `aten.` 행 수(193)와도 일치(교차검증) |
| 8 | Fable Axis4, line 129 (434행 eval_frame hook 범위) | `torch/_dynamo/eval_frame.py` (v2.13.0) | 200 | 부분지지 | `__enter__`/`__exit__` 메서드 존재 확인(L883, L897)했으나 "compile된 함수 호출 범위에서만"이라는 정확한 스코프 로직까지는 깊이 검증하지 못함 |
| 9 | Fable Axis4, line 129 (439행 trace_rules.py 존재) | `torch/_dynamo/trace_rules.py` (v2.13.0) | 200 | 지지 | 파일이 v2.13.0 태그에 실존(HTTP 200) |
| 10 | Fable Axis4, line 129 (454행 guard tensor 메타데이터, overview 91-98행) | `docs/source/user_guide/torch_compiler/torch.compiler_dynamo_overview.md` (v2.13.0) | 200 | 지지 | L89-97: "Python class... dtype... device... requires_grad... dispatch_key... ndim... sizes*... strides*" — 목록·줄 번호 거의 정확히 일치 |
| 11 | Fable Axis4, line 129 (496-502행 skip 조건) | `.../compile/programming_model.skipped_functions.md` (v2.13.0) | 200 | 지지 | "the skip is only applied to the current function and NOT any nested function calls. `torch.compile` will still attempt to compile nested calls." |
| 12 | Fable Axis4, line 129 (679-691행 API 존재) | `functorch/compile/__init__.py`, `torch/_dynamo/backends/{common,registry}.py` (v2.13.0) | 200 | 지지 | `make_boxed_func`(functorch/compile), `def aot_autograd(**kwargs)`(backends/common.py L133), `def register_backend`/`def list_backends`(backends/registry.py L85,L141) 모두 실존 |
| 13 | Fable A4-1, line 131/699 (Prims "123개"→"≤87개" 정정, 2.0 발표 "~250") | `torch/_prims/__init__.py`(v2.13.0), `docs.pytorch.org/docs/2.13/.../torch.compiler_ir.html`, `pytorch.org/get-started/pytorch-2.0/` | 200(전부) | **불일치**(개수 정정) / 지지(~250 부분) | 2.13 공식 문서 Prims IR 표: 정규식으로 스키마 행 카운트 **123개**(강의 원문과 정확히 일치). 2.0 발표문: "PrimTorch canonicalizes ~2000+ ... down to ~250 primitive operators" — ~250은 정확히 확인됨. Fable의 88/≤87은 근거 불명 |
| 14 | Fable A4-3, line 133 (213/228행 outstanding trace buffer 정의) | Rotenberg et al. 1999, §2.2 (p.113) | 200(PDF) | 지지 | "The outstanding trace buffers ... are used to 1) construct new traces that are not in the trace cache and 2) track branch outcomes ... allowing detection of mispredictions and repair" — "유망 후보 선별"이 아니라 "조립+오예측 복구" |
| 15 | Codex A1-4, line 655/160 | `docs.pytorch.org/docs/2.14/.../torch.compiler_custom_backends.html#overview` | 200 | 지지 | 페이지 도달 확인, "Backend Object" 대신 callable 계약 설명 섹션 존재(내용상 A4-14와 동일 페이지) |
| 16 | Codex A3-5, line 386/187 | `docs.python.org/3.11/whatsnew/3.11.html#cheaper-lazy-python-frames` | 200 | 지지 | "Old-style frame objects are now created only when requested by debuggers or ... `sys._getframe()`... For most user code, no frame objects are created at all." |
| 17 | Codex A3-9, line 510-515/192 | `raw.../v2.13.0/torch/_dynamo/cache_size.py` | 200 | 지지 | 캐시가 `f_code`의 `co_extra`(코드 객체)에 귀속됨(주석 L16-18) — "PyFrameObject로 감싸 호출별 frame에 저장되는 것처럼" 보이는 그림 구조가 실제 구조와 다름 |
| 18 | Codex A3-10, line 570-591/193 | `raw.../v2.13.0/torch/_inductor/codegen/triton.py` | 200 | 지지 | `def codegen_kernel(...) -> str:` docstring: "Convert the TritonKernel from Inductor SIMD IR to triton code" (L6336-6340) — Triton은 코드 문자열을 받음, IR을 직접 받지 않음 |
| 19 | Codex A4-1, line 305-309/197 | `docs.pytorch.org/docs/2.9/generated/torch.jit.trace.html` | 200 | 지지 | "You must provide example inputs, and **we run the function**, recording the operations performed on all the tensors." |
| 20 | Codex A4-2, line 300-313/198 | `docs.pytorch.org/docs/2.9/generated/torch.jit.script.html` | 200 | 지지 | "will **inspect the source code, compile it as TorchScript code** using the TorchScript compiler" |
| 21 | Codex A4-3, line 341/199 | `docs.pytorch.org/docs/2.9/jit.html` | 200 | 지지 | "optimized"/"optimize" 관련 언급 다수, jit.trace 설명에 "will be optimized using just-in-time compilation" — TorchScript에 최적화 경로가 있음을 뒷받침 |
| 22 | Codex A4-5, line 536/201 | `raw.../v2.13.0/cache_size.py` + `docs.pytorch.org/docs/main/.../programming_model.recompilation.html#changing-the-cache-size-limit` | 200(둘 다) | 지지 | cache_size.py 상동(행 #1). recompilation 문서: "all nested function calls **WILL be skipped** (Dynamo will try to use previously compiled bytecode for the nested functions)" — main 문서지만 v2.13.0 스냅샷과 대조해 내용 동일함을 직접 diff로 확인 |
| 23 | Codex A4-7, line 190-207/203 | Intel Technology Journal Q1 2001, "Trace Cache" 절 | 200(PDF) | 지지 | "The Trace Cache is the primary or Level 1 (L1) instruction cache ... Only when there is a Trace Cache miss does the NetBurst microarchitecture fetch and decode instructions from the Level 2 (L2) cache." — 별도 L1 I-cache가 아니라 L2 경로 |
| 24 | Codex A4-8, line 213-229/204 | Rotenberg et al. 1999 §2.2 (p.113) | 200(PDF) | 지지 | 행 #14와 동일 인용. "후보 선별" 아님, "trace 조립+오예측 복구" |
| 25 | Codex A4-9, line 192/205 | Rotenberg et al. 1999 Abstract·§1 (p.111) | 200(PDF) | 지지 | Abstract: "instruction fetch bandwidth requirements will also increase... Conventional instruction caches hinder this effort because long instruction sequences are not always in contiguous cache locations." bank conflict/cache miss 확률 언급 없음 |
| 26 | Codex A4-10, line 353/206 | `docs.pytorch.org/docs/main/.../programming_model.skipped_functions.html` | 200 | 지지 | 행 #11과 동일 문서 계열. "완전 지원"이 아니라 skip/capture 범위가 있음을 뒷받침 |
| 27 | Codex A4-11, line 380/207 | `docs.python.org/3.11/whatsnew/3.11.html#inlined-python-function-calls` | 200 | 부분지지 | "Python will call an evaluating C function to interpret that function's code" — eval loop이 별도 C 함수라는 취지는 뒷받침하나, "frame이 eval loop를 담지 않는다"를 문자 그대로 확인해주는 문장은 아님(간접 지지) |
| 28 | Codex A4-12, line 548/208 | `docs.python.org/3.11/whatsnew/3.11.html#pep-659-specializing-adaptive-interpreter` | 200 | 지지 | "Python will then replace the current operation with a more specialized one" — CPython 3.11+에 adaptive specialization 존재, "기록/최적화 없음" 주장과 배치 |
| 29 | Codex A4-13, line 538/209 | `raw.../v2.13.0/torch/_dynamo/config.py` + `docs.pytorch.org/docs/2.14/generated/torch.compile.html` | 200(둘 다) | 지지 | config.py L185-186 `automatic_dynamic_shapes` 기본 True(env `"1"`). torch.compile 문서: "`dynamic=True`... generate a kernel that is as dynamic as possible... `dynamic=False`... we will NEVER generate dynamic kernels... By default (None), we automatically detect" — 인용과 정확히 일치 |
| 30 | Codex A4-14, line 657-670/210 | `docs.pytorch.org/docs/2.14/.../torch.compiler_custom_backends.html#overview` | 200 | 지지 | 행 #15와 동일 페이지, custom backend 최소 예제 계약 설명 확인 |
| 31 | Codex A4-15, line 677-688/211 | `raw.../v2.13.0/torch/_functorch/aot_autograd.py` + `docs.pytorch.org/docs/2.14/.../torch.compiler_custom_backends.html#custom-backends-after-aotautograd` | 200(둘 다) | 지지 | 소스 L470 `aot_autograd_decompositions: dict = {}`(기본 빈 dict), L510 `decompositions = aot_config.decompositions or {}`. 문서: "AOTAutograd produces FX graphs consisting of core Aten ops." — 문서는 무조건적으로 서술하지만 소스상 기본값은 빈 dict라는 Codex의 "needs verification" 근거가 실재함 |
| 32 | Codex A4-16, line 619/212 | `docs.pytorch.org/docs/stable/.../torch.compiler_ir.html` | 200(리다이렉트) | 지지 | 응답 본문이 JS/meta-refresh: `location.replace("../../../2.14/.../torch.compiler_ir.html"...)` — stable이 현재 2.14로 이동함을 직접 확인 |
| 33 | Codex A4-17, line 651/213 | `docs.pytorch.org/docs/2.14/generated/torch.compile.html` | 200 | 지지 | 행 #29와 동일 페이지. guard 통과 시 재사용, 새 조건 시 추가 컴파일이라는 서술과 일치 |
| 34 | Codex 검증범위 요약, line 317/217 | `pytorch.org/blog/pytorch-2-12-release-blog/` | 200 | 지지 | "Torchscript is now Deprecated — Torchscript was deprecated in **2.10** and `torch.export` should be used to replace the jit trace and script APIs" — 정확히 일치 |

### 요약
- 총 34건: 지지 30 / 부분지지 2(#8, #27) / 불일치 2(#2, #13 개수 부분) / 무관 0 / 접근불가 0
- **가장 중요한 하향 조정**: 병합 요약 12·13·54행과 Fable A4-1(line 699, "high" 확신)의 "Prims 123개는 틀렸고 실제 ≤87개"라는 정정 자체가 틀렸다. v2.13.0 소스의 `__all__`은 127개(비연산 1개 제외 126개)이고, PyTorch가 실제 배포한 2.13 버전 공식 문서의 Prims IR 표는 정확히 **123개** 행을 담고 있어 강의 원문 숫자와 일치한다. "Codex는 needs verification 처리"라고 적힌 부분이 오히려 맞았다 — 이 항목은 리뷰 문서에서 **삭제/역정정**해야 한다.
- 나머지 불일치·무관 항목 없음. Codex가 잡은 high 항목들(cos().cos() 예제, accumulated_recompile_limit 범위, Pentium 4 trace cache miss 경로, outstanding trace buffer 정의, jit.trace/jit.script 오분류, dynamic=True/False 의미, stable→2.14 링크 이동)은 모두 1차 소스로 재확인되어 그대로 반영 가능.
- #8(eval_frame.py hook 범위), #27(frame/eval loop 분리 근거로 쓰인 3.11 changelog)은 인용 자체는 유효하지만 주장 전체를 문자 그대로 뒷받침하는 문장까지는 확인하지 못해 부분지지로 낮춤 — 결론을 바꾸지는 않음.

## 원문 리뷰

<details><summary>Fable 5.1 전문</summary>

# Review: `src/content/lectures/03-graph-mode.mdx` (Week 3, PyTorch Graph Mode)

## TL;DR

- **Axis 1 (AI slop/용어)**: 대체로 깨끗함. 음차("컨트롤 플로우 그래프", "상징 실행")와 표기 불일치(FxGraph/FX Graph, Super-scalar/superscalar), "핵심" 6회 정도가 전부. 비표준 용어는 "Trace Replay"/"Trace Generation" 정도.
- **Axis 2 (이해도/장황함)**: 가장 큰 문제. Dynamo의 guard→실행→graph break→resume 루프가 **다섯 번**(390-408, 450-465, 467 mermaid, 506-520 SVG, 522-536, 540-563) 반복됨. Trace cache 절(190-231)은 강의 주제 대비 과도하게 길다. `fullgraph`, joint graph/partitioning/functionalization 등 정의 없는 용어가 있음.
- **Axis 3 (다이어그램)**: 14개 중 10개는 문제없음. IR lowering mermaid(570)의 "Loop-Level IR → Triton" 엣지가 틀렸고, slide22_1(442)은 슬라이드 크기에서 판독 불가이며 본문 5단계 순서와 그림 라벨이 어긋남. 467 mermaid는 recompile 후 실행으로 돌아가는 엣지가 없음.
- **Axis 4 (기술 정확성)**: v2.13.0 소스/문서로 대조한 결과 recompile_limit 계열, Core Aten 193개, TorchScript 2.10 deprecation, automatic dynamic(2.1), skip 규칙, aot_autograd/make_boxed_func API는 모두 정확. **틀린 것**: Prims "123개"(실제 ≤87), "frame 내부에 evaluation loop이 존재", AOTAutograd가 frontend인데 "백엔드 과정에 포함", outstanding trace buffer의 역할, `jit.script`≈partial evaluator.
- **우선 수정 3건**: (1) Dynamo 루프 5중 반복을 1개 다이어그램 + 1개 정리로 통합, (2) Prims 개수·frame/eval loop·Loop-level IR→Triton 등 사실 오류 정정, (3) slide22_1 교체 또는 크롭 + 본문 단계 순서를 그림과 일치.

---

## Axis 1 — AI slop 및 용어

- **[A1-1] [severity: low] line 110** — 인용: "위 소스 코드를 그래프 형태로 표현한 것이 **컨트롤 플로우 그래프(CFG)** 입니다." → 문제: 덱 전체가 영어 용어를 쓰는데 여기만 음차. 바로 앞 90행은 "Control Flow Graph(CFG)"로 씀. → 제안: "**Control Flow Graph(CFG)**입니다."
- **[A1-2] [severity: medium] line 182, 361, 456** — 인용: "Python bytecode를 상징적으로 분석하고" / "bytecode instruction을 순서대로 상징 실행하면서" / "example input을 상징적으로 분석해" → 문제: "상징 실행"은 symbolic execution의 직역으로 대상 독자에게 정의 없이 등장하며, 세 곳의 표현이 다 다름. → 제안: 361행에서 한 번 정의("실제 값을 계산하지 않고 값의 종류·shape만 추적하며 bytecode를 한 줄씩 해석하는 symbolic execution")하고 이후는 "symbolic execution"으로 통일.
- **[A1-3] [severity: medium] line 450, 506, 522** — 인용: "#### Trace 생성 과정" / "#### Trace Generation의 결과물" / "#### Trace Replay" → 문제: "Trace Replay", "Trace Generation"은 Dynamo 문서·코드에 없는 용어. Dynamo는 cache entry / guard / recompile / resume function이라는 용어를 씀. 강의가 trace 프레임으로 설명하는 것 자체는 좋지만 소제목까지 만들어 쓰면 독자가 실제 문서를 찾을 때 연결이 안 됨. → 제안: 소제목을 "Cache entry의 구성 요소", "Cache entry 재사용과 recompile"로 바꾸고, 450행 근처에 "이 강의의 'trace' = Dynamo의 captured graph + transformed bytecode"라고 한 줄 매핑.
- **[A1-4] [severity: low] line 231** — 인용: "결과적으로 Trace Cache는 분기 명령으로 인해 발생하는 명령어 fetch의 불연속성을 완화함으로써 fetch 효율을 높이고, 연속적인 명령어 공급을 가능하게 하여 전체 실행 성능 향상에 기여합니다." → 문제: 205-220행을 그대로 되풀이하는 요약형 closer. → 제안: 삭제.
- **[A1-5] [severity: low] line 259** — 인용: "이처럼 반복적으로 수행되는 복잡한 연산을 미리 계산하여 실행 성능을 높일 수 있습니다." → 문제: 코드 예제가 이미 말한 것을 다시 말하는 filler. → 제안: 삭제.
- **[A1-6] [severity: low] line 24, 81, 268, 321, 518, 540** — 인용: "오늘의 핵심 주제" / "오늘의 핵심 주제인 Dynamo" / "핵심 아이디어" / "핵심 전제" / "핵심" / "핵심 정리" → 문제: "핵심" 6회. → 제안: 81행 "오늘의 핵심 주제인" 삭제(24행과 중복), 268행 "핵심 아이디어를" → "아이디어를", 518행 "계산 수행이 핵심" → "계산 수행".
- **[A1-7] [severity: low] line 159, 172** — 인용: "superscalar CPU는" / "Super-scalar processor의" → 문제: 표기 불일치. 같은 문제로 "FxGraph"(25, 446, 472, 572), "FX Graph"(448, 461), "FX graph"(392, 456)가 섞여 있고, "AOTAutograd"(78, 583)와 "AOT Autograd"(572)도 다름. → 제안: "superscalar", "FX graph"(코드 식별자 `FxGraph`가 아닐 때), "AOTAutograd"로 통일.
- **[A1-8] [severity: low] line 132** — 인용: "실제 컴파일러에서는 여러 instruction의 시퀀스가 하나의 노드를 이룹니다." → 문제: 표준 용어 **basic block**을 쓰지 않아 독자가 검색으로 이어갈 수 없음. → 제안: "…하나의 노드(**basic block**)를 이룹니다."
- **[A1-9] [severity: low] line 301** — 인용: "CPython에 직접 통합, bytecode 수준 tracing" → 문제: 345행에서 "CPython에 내장된 plugin이 아니라 … PEP 523을 사용하는 C extension"이라고 스스로 정정함. 표에서 "직접 통합"은 오해를 유도. → 제안: "PEP 523 frame evaluation hook, bytecode 수준 tracing".

## Axis 2 — 이해도, ELI5 균형, 장황함

- **[A2-1] [severity: high] line 390-408, 450-465, 467-492, 506-520, 522-536, 540-563** — 인용: "분할된 trace들은 다음과 같은 방식으로 실행됩니다" / "Graph break 이후, resumed function의 bytecode를 가지고 위의 과정을 recursive하게 반복합니다." / "그 결과물은 다음과 같은 구조입니다" / "#### Trace Replay" / "#### 핵심 정리: Dynamo의 원리" → 문제: guard 검사 → transformed bytecode 실행 → FX graph 계산 → graph break → resume function → 재귀라는 동일한 루프가 산문 2회, 번호 목록 3회, mermaid 1회, SVG 1회로 반복됨. "recursive"라는 말도 408, 465, 555행에 세 번. 독자는 매번 "이건 앞과 뭐가 다른가"를 확인하느라 시간을 씀. → 제안: (a) 390-408을 torch-dynamo SVG + 4단계 목록으로 유지, (b) 450-465 "Trace 생성 과정"을 cache entry 구성 요소 설명으로 유지, (c) 467 mermaid와 510 SVG 중 하나만 남기고(권장: 510 SVG 삭제, 467 mermaid를 A3-7대로 수정), (d) 506-520 삭제, (e) 522-538 "Trace Replay"는 의사코드 + recompile_limit 문단만 남기고 "핵심 정리"(540-563) 안으로 합침.
- **[A2-2] [severity: medium] line 190-231** — 인용: "#### Trace Cache (Pentium 4에 적용된 사례)" → 문제: Dynamo 강의에서 42행, 그림 3개(SVG 1 + PNG 2), 하드웨어 장치 3개, 4단계 흐름까지 다룸. 필요한 교훈은 "hot path를 연속 저장해 두고 예측이 맞으면 통째로 재사용, 틀리면 빠져나온다"뿐. → 제안: slide10_2(217)와 219-220행 두 줄, 그리고 205-207행 3단계만 남기고 209-213, 222-231, 196-201 SVG는 삭제. 논문 인용은 유지.
- **[A2-3] [severity: medium] line 237-277** — 인용: "Static input은 더 이상 필요 없음 - 영향이 이미 반영되었기 때문" / "Static input에만 영향을 받는 코드는 다시 계산할 필요가 없음" → 문제: partial evaluation 정의(237-242) → 그림(244) → 코드(250-257) → 두 번째 그림(261) → 그림 재설명(268) → 그림의 callout을 다시 bullet으로(272-273) → 일반론(275)으로 같은 개념을 5단계로 늘림. → 제안: 268-275를 "위 그림처럼 static input에만 의존하는 계산(주황)은 한 번 계산해 특화 프로그램에 넣고, 실행 시에는 dynamic input만 받는다. 단 static이라고 가정한 값이 바뀌면 특화 프로그램을 다시 만들어야 한다."로 압축.
- **[A2-4] [severity: medium] line 309, 321** — 인용: "tensor 연산은 dynamic input이고 Python 코드의 제어 흐름은 static input이라고 가정" / "\"tensor 연산은 dynamic input, Python 제어 흐름은 static input\"이라는 가정" → 문제: partial evaluation 비유를 `jit.trace`에 적용하면서 "연산"과 "입력"을 섞음. `jit.trace`가 dynamic으로 남기는 것은 tensor의 **값**이고, static으로 굳히는 것은 제어 흐름과 그 결정에 쓰인 값(shape, Python scalar)임. 지금 문장은 "연산이 입력"이라는 뜻이 되어 비유가 어긋남. → 제안: "tensor **값**은 dynamic input, Python 제어 흐름(그리고 그 분기를 결정한 값)은 static input이라고 가정".
- **[A2-5] [severity: medium] line 446** — 인용: "Bytecode analysis → bytecode transformation → FxGraph construction → user-defined compilation → guard insertion의 과정을 거칩니다." → 문제: 이 5단계 이름은 그림(442)의 라벨(Python Bytecode Analysis, Guard, Captured Computation Graph, Transformed Bytecode, Resume Functions)과 이름·순서가 다르고, "user-defined compilation"은 653행에서야 설명되는 개념. → 제안: 그림 라벨 순서대로 "bytecode 분석 → guard 생성 → FX graph 캡처 → (backend 컴파일) → transformed bytecode + resume function 생성"으로 쓰고, backend 컴파일은 "Device Backend 통합 절에서 다룸"이라고 명시.
- **[A2-6] [severity: medium] line 496** — 인용: "`fullgraph=False`인 경우에도 항상 resume bytecode를 만들 수 있는 것은 아닙니다." → 문제: `fullgraph` 옵션이 덱에서 처음이자 유일하게 등장하며 정의 없음. → 제안: 앞에 한 줄 추가: "`torch.compile(fullgraph=True)`는 graph break가 생기면 에러를 내고, 기본값 `fullgraph=False`는 graph break 지점에서 분할해 계속 진행합니다."
- **[A2-7] [severity: medium] line 583** — 인용: "joint graph 생성과 forward/backward 분리(partitioning)를 수행합니다. … normalization(functionalization + decomposition)은 동일하게 수행합니다." → 문제: joint graph, partitioning, functionalization, normalization 네 용어가 정의 없이 한 문단에 등장. 78행에서 AOTAutograd는 Week 4로 미룬다고 했음. → 제안: "학습이면 backward graph를 함께 만들고 forward/backward로 나눕니다. 추론이어도 이 단계는 거치며, in-place 연산 제거(functionalization)와 decomposition은 항상 수행합니다. 자세한 내용은 Week 4." 정도로 줄이고 나머지는 삭제.
- **[A2-8] [severity: medium] line 727-729** — 인용: "**Q**: TorchDynamo는 Python에서만 사용되나요? **A**: TorchDynamo의 상당히 많은 부분이 Python으로 작성되어 있습니다." → 문제: 질문(사용 범위)과 답(구현 언어)이 어긋남. → 제안: "**A**: 네. Dynamo는 Python bytecode를 분석하므로 Python으로 작성된 모델에만 적용됩니다(C++ frontend나 이미 export된 그래프에는 해당 없음). 구현 자체는 대부분 Python이고, frame evaluation hook과 guard 검사만 C/C++ extension입니다."
- **[A2-9] [severity: low] line 723-725** — 인용: "**A**: Tracing은 전체 과정을 추상적으로 표현한 것이고, bytecode static analysis는 tracing의 일부 과정에 해당합니다." → 문제: 답이 모호하고, Dynamo의 분석은 example input을 가진 symbolic execution이라 엄밀히 "static analysis"도 아님. → 제안: "Dynamo는 실제 입력(example input)의 메타데이터를 가지고 bytecode를 한 줄씩 symbolic execution합니다. 입력 없이 코드만 보는 static analysis와 달리 값에 의존하는 분기를 그 입력 기준으로 결정하고, 그 결정을 guard로 기록합니다. 그래서 tracing에 가깝습니다."
- **[A2-10] [severity: low] line 651 vs 717, 673 vs 733** — 인용: "첫 번째 실행 시에는 tracing과 compilation이 동시에 진행되어 오버헤드가 있을 수 있지만" / "Inductor를 backend로 선택하는 것도 가능하며, 별도의 backend를 붙여서 수행하는 것도 가능합니다." → 문제: Q&A 두 항목이 본문 문장을 거의 그대로 반복. → 제안: 715-717, 731-733 Q&A 삭제(본문이 이미 답함).
- **[A2-11] [severity: low] line 132 vs 112-129** — 인용: "이 예제에서는 코드 한 줄이 하나의 노드에 해당하지만" → 문제: 바로 위 CFG는 L1-2를 한 노드로, L3/L5/L7 세 조건을 D 하나로 합쳤고, J1은 본문에서 설명하지 않음. → 제안: "이 예제에서는 연속된 줄(L1-2)과 세 조건문(L3/L5/L7 → D)을 각각 한 노드로 묶었고, J1은 세 분기가 다시 합류하는 지점입니다."

## Axis 3 — 다이어그램

문제 없음(한 줄):
- line 141 mermaid(trace 예시): d=0 경로가 CFG 노드 이름과 일치. 좋음.
- line 150 trace-diagram SVG: Program+입력 → 실행 → 입력별 trace. 160행 "입력 의존적" 주장을 그대로 보여줌.
- line 217 slide10_2: 219-220행 설명과 정확히 대응.
- line 224 slide10_1: Rotenberg 논문 원도. 226-229행 4단계와 대응(단 A4-3 참조).
- line 244 slide11_1: Jones의 mix 다이어그램. 본문 `p`, `in1`, `in2`, specialized program 용어 일치.
- line 365 slide14_1: 일반적 CPython 파이프라인. 370-371행과 대응.
- line 394 torch-dynamo SVG: Compilation/Execution 두 행, Break/Check 표시. 401-406행과 대응. 좋음.
- line 414 slide21_1: 공식 Dynamo overview 그림. 420-430행 좌/우 설명과 대응.

문제 있음:
- **[A3-1] [severity: medium] line 69 + line 59** — 인용: "**Autograd 통합**: 머신러닝을 위한 컴파일러이므로 자동 미분 기능이 백엔드 과정에 포함" → 문제: slide03_1에서 AOTAutograd 박스는 "Backend" 라벨 및 "Integration Interface" 점선 **위**, 즉 frontend 쪽에 있음. 본문은 백엔드에 포함된다고 함. → 제안: "**AOTAutograd**: 자동 미분(backward graph 생성)이 backend에 넘기기 전 frontend 단계에 포함".
- **[A3-2] [severity: low] line 112-129 mermaid(CFG)** — 인용: "L12[\"L1-2<br/>input❨a,b,c❩<br/>d ← b∗b - 4∗a∗c\"]" → 문제: 노드 7개, 가독성은 괜찮음. 다만 본문 132행과 노드 단위가 어긋남(A2-11). → 제안: A2-11 문장 수정으로 해결.
- **[A3-3] [severity: low] line 196-201 trace-cache SVG** — 인용: (SVG 텍스트) "반복되는 구간|반복되는 구간|반복되는 구간" → 문제: 셀 row 하나에 주황 구간 3개를 표시한 장식 그림. trace cache의 메커니즘(비연속 블록 → 연속 저장)은 바로 아래 slide10_2가 보여주므로 중복. → 제안: 삭제(A2-2와 함께).
- **[A3-4] [severity: low] line 261-266 partial-evaluation SVG + line 272-273** — 인용: (SVG) "① 재계산 불필요" / "② static 효과 반영됨" / "③ 불필요" → 문제: 그림의 callout은 ①②③ 세 개인데 본문 bullet은 두 개라 번호 매핑이 없고, ①("재계산 불필요")이 partial evaluation **이전**(위쪽) row에 붙어 있어 "아직 최적화 전인데 왜 불필요?"로 읽힘. → 제안: ①을 아래 row(최적화 후)로 옮기거나 "static-only 계산(제거 대상)"으로 바꾸고, 본문 bullet을 ①②③에 맞춰 세 개로 쓰거나 번호를 없앰.
- **[A3-5] [severity: medium] line 386 slide15_1 + line 377-384** — 인용: alt "Call stack이 frame object의 linked list로 표현되는 Python VM 구조 다이어그램" → 문제: (a) 그림은 2016년 블로그의 CPython ≤3.10 모델(`PyFrameObject`가 `f_back`으로 연결, `f_locals/f_globals/f_builtins`). CPython 3.11+에서는 `_PyInterpreterFrame`이 thread별 data stack에 놓이고 `PyFrameObject`는 필요할 때만 생성되는데, 2.13의 Dynamo가 지원하는 Python은 전부 3.10 이상. (b) 본문의 계층 6개(Evaluation loop/Frame/Call Stack/Thread/Interpreter/Runtime) 중 그림은 Frame/Call Stack만 보여줌. (c) 그림 오타 "Other filed". → 제안: 그림 캡션에 "CPython 3.10 이전 구조(개념 설명용)"를 달고, 본문 목록을 Frame과 Call Stack 두 항목으로 줄이거나 그림을 3.11+ 구조로 교체.
- **[A3-6] [severity: medium] line 442 slide22_1** — 인용: alt "Bytecode 분석부터 guard 삽입까지 Dynamo의 내부 흐름 다이어그램" → 문제: 원본 2048×1823 그림을 `h-96`(384px)으로 넣어 코드 텍스트가 판독 불가. 그림의 라벨(Source Code, Input, Python Bytecode Analysis, Guard, Captured Computation Graph, Transformed Bytecode, Resume Functions)과 446행 5단계 명칭·순서가 다름(A2-5). 그림에는 "user-defined compilation" 단계가 없고, 본문에는 Resume Functions가 없음. → 제안: 그림을 상단(Source Code → Bytecode Analysis → Guard/Captured Graph/Transformed Bytecode)만 크롭해 넣거나 depyf 링크로 대체하고, 446행을 그림 라벨 순서로 재작성.
- **[A3-7] [severity: medium] line 467-492 mermaid** — 인용: "PF[\"PyFrameObject 호출\"]" / "RECOMPILE[\"recompile_and_add_another_cache_entry()<br/>새 Cache Entry 추가\"]" → 문제: (a) recompile 후 새 entry로 실행이 이어지는 엣지(RECOMPILE → TB)가 없어 흐름이 끊김. (b) `RF1`, `RF2`가 같은 라벨 "Resumed function"으로 두 번 등장해 의미가 없음(분기별 continuation을 뜻한다면 라벨에 "분기 A/분기 B"). (c) "PyFrameObject 호출"은 frame이 호출되는 것이 아니라 함수 호출로 frame이 생성되는 것. (d) SKIP 노드는 536행의 "기존 entry는 계속 재사용"을 표현하지 않음. → 제안: `RECOMPILE --> TB` 추가, RF 라벨을 "Resumed function (분기 A)"/"(분기 B)"로, PF를 "함수 호출 → 새 frame", SKIP을 "새 컴파일 중단<br/>(기존 entry는 guard 통과 시 계속 사용)"으로.
- **[A3-8] [severity: low] line 510-515 trace-generation SVG** — 인용: (SVG) "(recompile_limit 8 초과 시 eager skip)" (font-size 8.5) / "(resume 불가 시 frame 전체 skip)" (font-size 9) → 문제: `h-80`으로 축소되면 8.5–9px 주석은 읽히지 않음. 또 "Transformed bytecode" 상자에서 곧바로 "FxGraph 계산"으로 화살표가 내려가고 guard는 옆에서 합류해, guard 검사가 bytecode 실행 뒤/도중으로 읽힘(실제로는 C 쪽 eval frame에서 bytecode 실행 전에 검사). 467 mermaid와 내용이 같으므로 A2-1대로 하나만 남기는 것을 권장. → 제안: 삭제하거나, 남긴다면 guard 상자를 흐름 맨 위로 옮기고 주석 폰트를 11px 이상으로.
- **[A3-9] [severity: medium] line 570-575 mermaid(IR lowering)** — 인용: "I -->|Inductor Loop-Level IR| T[\"Triton\"]" → 문제: Loop-level IR은 Inductor **내부** IR이고(slide03_1도 Inductor 상자 안에 "Inductor Loop-level IR"로 표시), Triton이 받는 것은 Inductor codegen이 생성한 **Triton kernel 소스**임. 591행 "백엔드는 전달받은 low-level IR을 … 변환하여 실행"도 같은 오류. → 제안: 엣지 라벨을 "생성된 Triton kernel 코드"로, 591행을 "Inductor가 loop-level IR에서 Triton(GPU)·C++/OpenMP(CPU) kernel 소스를 생성하고, Triton 컴파일러가 이를 PTX 등 device 코드로 컴파일해 실행합니다."로.

## Axis 4 — 기술 정확성

검증에 사용한 기준: pytorch `v2.13.0` 태그의 `torch/_dynamo/config.py`, `convert_frame.py`, `torch/csrc/dynamo/eval_frame_cpp.cpp`, `torch/_dynamo/backends/common.py`, `torch/_dynamo/__init__.py`, `functorch/compile/__init__.py`, `aten/src/ATen/native/native_functions.yaml`, `torch/_prims/__init__.py`, `docs/source/scripts/build_opsets.py`, `docs/source/user_guide/torch_compiler/{torch.compiler_dynamo_overview,torch.compiler_custom_backends,torch.compiler_dynamic_shapes}.md`, `compile/programming_model.{skipped_functions,recompilation}.md`, 그리고 `v2.9.0`/`v2.10.0`의 `torch/jit/_trace.py`.

검증되어 **문제 없는** 주장(참고): 536행 `recompile_limit=8`, `accumulated_recompile_limit=256`, `cache_size_limit` alias, "상한 초과 후 기존 entry는 guard 통과 시 재사용"(2.13은 `FrameAction.RUN_ONLY`로 처리, `convert_frame.py:2035-2040`); 317행 "2.10에서 deprecated"(`torch.jit.trace is deprecated` DeprecationWarning이 v2.10.0 `_trace.py`에 추가, v2.9.0에는 없음); 599행 Core Aten 193개(`native_functions.yaml` `core` 태그 193개, `avg_pool2d`·`convolution` 포함); 538행 automatic dynamic 2.1부터, `dynamic=None` 기본; 434행 compile된 함수 호출 범위에서만 hook(`eval_frame.py` `__enter__/__exit__`); 439행 `torch/_dynamo/trace_rules.py` 존재; 454행 guard가 검사하는 tensor 메타데이터 목록(overview 문서 91-98행과 일치); 496-502행 skip 조건(문서와 일치); 679-691행 `aot_autograd(fw_compiler=, bw_compiler=)`, `make_boxed_func`, `register_backend`, `list_backends()` 모두 존재.

- **[A4-1] [severity: high] line 699** — 인용: "Prims까지 내려가면 약 120개(2.13 시점 123개)로 추려집니다." → 문제: 2.13 문서의 Prims IR 표는 `docs/source/scripts/build_opsets.py::get_prims()`가 `torch._prims.__all__`의 OpOverload만 세어 만드는데, v2.13.0 `torch/_prims/__init__.py`의 `__all__`은 88개(그중 `RETURN_TYPE`는 op가 아님)이므로 상한이 87개. 123은 어디서도 나오지 않음. PyTorch 2.0 발표문은 Prims를 "~250개"로 소개했음. 확신: high(개수), medium(발표문 250). → 제안: "Prims까지 내려가면 90개 미만(2.13 `torch._prims` 기준 87개; 2.0 발표 시점에는 약 250개로 소개)으로 추려집니다." 600행 "내부 operation 수는 PyTorch 버전에 따라 달라집니다"는 유지.
- **[A4-2] [severity: high] line 380** — 인용: "각 함수 호출 시 새로운 frame이 생성되며, frame 내부에 code object와 evaluation loop이 존재" → 문제: evaluation loop(`_PyEval_EvalFrameDefault`)은 interpreter의 함수이며 frame을 **인자로 받아** 실행함. frame 안에 있는 것은 code object 참조, locals, value stack, 이전 frame 링크임. PEP 523이 바로 "frame을 받아 실행하는 함수"를 교체하는 API라서 이 구분이 이 강의의 요지와 직결됨. 확신: high. → 제안: "각 함수 호출 시 새로운 frame이 생성되며, frame은 code object 참조·지역 변수·value stack을 담습니다. Evaluation loop은 이 frame을 받아 bytecode를 실행하는 함수(`_PyEval_EvalFrameDefault`)이고, PEP 523은 이 함수를 교체할 수 있게 합니다."
- **[A4-3] [severity: medium] line 213, 228** — 인용: "**Trace Buffer** (outstanding trace buffers): 모든 trace를 곧바로 cache에 넣지 않고, 성능 향상 효과가 클 것 같은 후보를 먼저 모아두고 선별하는 버퍼" / "충분한 trace가 모이면 trace buffer에서 선별하여 Trace Cache를 update" → 문제: Rotenberg–Bennett–Smith(1999)의 outstanding trace buffers는 trace cache miss 시 instruction cache에서 fetch되는 명령을 모아 **하나의 trace를 조립**하는 버퍼(fill unit)이며, trace가 완성되면 trace cache에 기록함. 여러 후보 중 "효과가 클 것 같은 것을 선별"하는 장치가 아님. 확신: medium(논문 본문 재확인 권장). → 제안: "**Outstanding trace buffers**: trace cache miss 시 instruction cache에서 하나씩 fetch되는 명령을 모아 새 trace를 조립하는 버퍼. trace가 완성되면 Trace Cache에 기록" / 228행 "trace가 완성되면 Trace Cache에 기록".
- **[A4-4] [severity: medium] line 313** — 인용: "Partial evaluator와 유사하게 동작하되, Python 전체가 아닌 subset만 처리할 수 있는 별도의 interpreter를 사용합니다." → 문제: `jit.script`는 Python AST의 subset을 TorchScript IR로 컴파일하는 AOT 컴파일러로, 제어 흐름을 그대로 보존하고 입력에 특화(specialize)하지 않음. 입력을 static/dynamic으로 나눠 특화하는 partial evaluator와는 반대 방향. 확신: high. → 제안: "`jit.trace`와 달리 실행하지 않고 Python 소스(AST)를 직접 컴파일해 제어 흐름까지 그래프에 남기지만, Python 전체가 아닌 subset만 처리하는 별도 컴파일러·interpreter를 사용합니다."
- **[A4-5] [severity: medium] line 677** — 인용: "AOTAutograd는 Core Aten opset으로 구성된 FX graph를 만들어내기 때문입니다." → 문제: 2.13 custom backends 문서가 같은 문장을 쓰지만, `aot_autograd(fw_compiler=..., bw_compiler=...)`처럼 `decompositions=`를 주지 않으면 `aot_module_simplified`는 functionalization만 하고 decomposition은 기본 소수(detach 등)만 적용하므로 결과는 functional **ATen** graph이지 Core Aten으로 제한되지 않음(예: 덱의 605-614행 목록에 있는 `aten.hardtanh`는 decomposition 없이는 그대로 남음). Inductor는 자체 decomposition table을 넘김. 확신: medium(needs verification: `aot_autograd(fw_compiler=print_gm)`으로 `F.hardtanh`를 컴파일해 op 확인). → 제안: "AOTAutograd에 decomposition table(예: `torch._decomp.core_aten_decompositions()`)을 함께 주면 Core Aten opset으로 구성된 FX graph를 받게 됩니다." 그리고 688행 예시에 `decompositions=torch._decomp.core_aten_decompositions()` 인자 추가.
- **[A4-6] [severity: medium] line 574, 591** — 인용: "I -->|Inductor Loop-Level IR| T[\"Triton\"]" / "백엔드는 전달받은 low-level IR을 백엔드 특화된 device representation으로 변환하여 실행합니다." → 문제: A3-9와 동일. Triton은 IR이 아니라 생성된 kernel 소스를 받음. 확신: high. → 제안: A3-9 참조.
- **[A4-7] [severity: medium] line 59** — 인용: "자동 미분 기능이 백엔드 과정에 포함" → 문제: A3-1과 동일. AOTAutograd는 Dynamo 다음, backend integration interface **앞**의 frontend 단계(slide03_1과 2.0 발표 자료 모두). 확신: high. → 제안: A3-1 참조.
- **[A4-8] [severity: medium] line 381, 386** — 인용: "**Call Stack** - 여러 개의 frame이 쌓여 함수 호출 관계를 관리" + slide15_1 → 문제: A3-5. CPython 3.11+에서는 `PyFrameObject` linked list가 아니라 `_PyInterpreterFrame` data stack이며, Dynamo가 hook하는 evaluation 함수는 3.11+에서 `_PyInterpreterFrame*`을 받음. 개념 설명으로는 무방하나 "현재 구조"로 읽히면 틀림. 확신: high. → 제안: "(CPython 3.10 이전 구조 기준; 3.11+는 내부 표현이 달라졌지만 개념은 같음)" 주석 추가.
- **[A4-9] [severity: low] line 504** — 인용: "이 경우에는 안쪽에서 호출되는 함수들까지 모두 skip되어 eager로 실행됩니다." → 문제: 2.13 문서(`programming_model.recompilation.md` 109-110행)와 코드(`FrameExecStrategy(RUN_ONLY, RUN_ONLY)`)에 따르면 안쪽 함수도 **새 컴파일만 중단**되고 이미 컴파일된 bytecode는 guard 통과 시 계속 사용됨. "모두 eager"는 과장. 확신: high. → 제안: "…안쪽에서 호출되는 함수들도 새 컴파일이 중단됩니다(이미 컴파일된 entry는 guard가 통과하면 계속 사용)."
- **[A4-10] [severity: low] line 538** — 인용: "`dynamic=True`/`dynamic=False`로 이 자동 전환을 강제 On/Off할 수 있습니다." → 문제: `dynamic=True`는 "자동 전환을 켜는" 것이 아니라 **첫 컴파일부터** 모든 dimension을 dynamic으로 가정(문서는 비권장), `dynamic=False`는 항상 static 특화. 확신: high. → 제안: "`dynamic=True`는 처음부터 dynamic shape으로 컴파일하고(권장하지 않음), `dynamic=False`는 항상 shape에 특화해 재컴파일합니다."
- **[A4-11] [severity: low] line 583** — 인용: "Torch IR이 보다 구체화된 **Aten IR** 또는 **Prims IR** 형태로 lowering됩니다." → 문제: Inductor 경로에서 AOTAutograd 출력은 (decomposition 적용된) ATen graph이고, Prims IR은 별도 decomposition/`torch._refs` 경로에서만 등장함. "또는 Prims IR"은 2.0 발표 그림을 그대로 옮긴 것. 확신: medium (needs verification: `TORCH_LOGS=aot_graphs`로 Inductor backend의 forward graph에 `prims.*` op가 있는지 확인). → 제안: "주로 **Aten IR**(Core Aten) 형태로 lowering됩니다. 더 낮은 Prims IR로 내리는 경로도 있습니다."
- **[A4-12] [severity: low] line 670** — 인용: "forward_output = model(input)" → 문제: `input`은 정의된 적이 없고(645행은 `my_input`) Python builtin을 가림. 그대로 실행하면 `TypeError`. → 제안: `forward_output = model(my_input)`.
- **[A4-13] [severity: low] line 301** — 인용: "CPython에 직접 통합" → 문제: A1-9. PEP 523 hook이며 CPython 수정 없음(345, 432행이 정확). → 제안: A1-9 참조.
- **[A4-14] [severity: low] line 192** — 인용: "Bank conflict가 발생하거나 cache miss로 원하는 데이터가 캐시에 없을 가능성이 높아지기 때문입니다." → 문제: trace cache의 동기는 "한 cycle에 여러 비연속 basic block을 fetch하려면 instruction cache에 다중 port/bank 접근이 필요해 bandwidth가 제한된다"는 것이지 cache miss율 상승이 아님. 확신: medium. → 제안: "분기가 잦으면 한 cycle에 비연속 블록 여러 개를 가져와야 하는데, 일반 instruction cache는 연속 주소만 한 번에 읽을 수 있어 fetch bandwidth가 제한되기 때문입니다."

</details>

<details><summary>Codex gpt-6-astra 전문</summary>

문체는 대체로 자연스럽지만, 반복되는 진행 설명과 일부 비표준 명칭을 줄이면 좋겠습니다(Axis 1).
초심자에게는 CPU 배경 설명이 길고 FX·guard·AOTAutograd의 정의가 늦습니다(Axis 2). 도식은 대부분 관련성이 있으나 실행 순서와 글자 대비를 수정해야 합니다(Axis 3).
기술적 수정이 필요합니다(Axis 4). 우선순위는 ① 분기가 바뀌지 않는 예제 수정 ② `jit.trace`의 실제 연산 실행 설명 수정 ③ 재컴파일 한도의 적용 범위 수정입니다.
733줄 전체, Mermaid 4개, PNG 8개, ThemeImage 5쌍을 검토했습니다. 저장소는 수정하지 않았으며, PyTorch 미설치로 예제 실행은 하지 못했습니다. 2.13 문서에 접근하지 못한 항목은 버전 태그 소스로 확인하거나 ‘needs verification’으로 구분했습니다.

## Axis 1 — AI 문체와 비표준 용어

- **[A1-1] [severity: medium] line 81** — 인용: "세부 흐름은 앞의 "오늘 다룰 주제들"과 같으며, 대부분의 시간은 오늘의 핵심 주제인 Dynamo의 동작 원리를 설명하는 데 할애합니다." → 문제: 이미 제시한 목차와 범위를 다시 설명하는 진행 문장입니다. → 제안: 앞 문장까지 합쳐 “이번 강의에서는 Dynamo의 graph capture를 중심으로 IR lowering과 custom compiler backend 연결을 다룹니다.”로 줄입니다.
- **[A1-2] [severity: medium] line 231** — 인용: "fetch 효율을 높이고, 연속적인 명령어 공급을 가능하게 하여 전체 실행 성능 향상에 기여합니다." → 문제: 앞의 동작 설명을 추상적인 효과 세 개로 반복하는 마무리입니다. → 제안: “Trace cache는 실행 경로에 따라 명령어를 묶어, 분기를 넘어서 fetch할 수 있게 합니다.”로 줄이거나 문단을 삭제합니다.
- **[A1-3] [severity: low] line 110** — 인용: "컨트롤 플로우 그래프(CFG)" → 문제: 제목과 주변 설명은 `Control Flow Graph`를 쓰는데 여기만 음역합니다. 339행의 “커널 퓨전”도 앞의 `kernel fusion`과 혼용됩니다. → 제안: 각각 `Control Flow Graph(CFG)`, `kernel fusion`으로 통일합니다.
- **[A1-4] [severity: medium] line 655** — 인용: "Backend Object를 생성하고 function을 구현하여 등록합니다." → 문제: `Backend Object`라는 필수 PyTorch API 타입이 있는 것처럼 읽힙니다. 표준 표현은 **custom backend callable**이며, 이 예제는 함수만으로 연결합니다. → 제안: “FX GraphModule과 example inputs를 받아 실행 가능한 callable을 반환하는 backend 함수를 구현하고, `torch.compile(..., backend=my_backend)`에 전달합니다.”로 교체합니다. [공식 backend 계약](https://docs.pytorch.org/docs/2.14/user_guide/torch_compiler/torch.compiler_custom_backends.html#overview)

`Torch IR`, `Core ATen IR`, `Prims IR`, `guard`, `resumed function`은 임의로 만든 용어로 판정하지 않았습니다. 530행의 `recompile_and_add_another_cache_entry()`도 524행에서 의사코드라고 명시했으므로 가짜 API 지적 대상이 아닙니다.

## Axis 2 — 이해도·비유·분량

- **[A2-1] [severity: medium] line 170-233** — 인용: "Out-of-Order Execution과 동적 최적화" → 문제: Dynamo를 설명하기 전에 instruction window, branch prediction, 두 종류의 cache, predictor, buffer를 연달아 배웁니다. 구현이 서로 다르다는 단서까지 반복되어, 비교 사례를 이해하는 비용이 본 주제보다 커집니다. → 제안: 본문에는 “관찰한 실행 정보를 재사용한다는 공통점만 있다”는 비교 한 문단을 남기고 trace cache의 하드웨어 구조는 부록으로 옮깁니다.
- **[A2-2] [severity: medium] line 182** — 인용: "tensor 연산을 FX graph로 생성합니다." → 문제: FX graph를 여러 번 사용한 뒤 448행에서야 “처음 등장하는” 개념으로 정의합니다. 앞에서 다룬 CFG와 tensor 데이터 흐름 그래프의 차이도 늦게 드러납니다. → 제안: 이 위치에 “FX graph는 tensor 연산과 데이터 의존 관계를 기록한 그래프입니다. 앞의 CFG는 조건에 따른 실행 경로를 표현합니다.”를 추가하고 448행의 정의를 당깁니다.
- **[A2-3] [severity: medium] line 403-406** — 인용: "현재 상태가 해당 trace를 수행할 수 있는 조건을 만족하는지 Guard로 검사" → 문제: ‘조건을 만족하는 조건 검사’만으로는 무엇을 검사하는지 알기 어렵습니다. 여기서는 생활 비유보다 작은 입력 예제가 효과적입니다. → 제안: “예를 들어 float32·shape `[10]`을 전제로 컴파일했다면 guard가 이 전제를 확인합니다. 값만 달라진 입력은 재사용할 수 있지만, shape 변경은 재컴파일을 유발할 수 있습니다.”를 붙입니다.
- **[A2-4] [severity: medium] line 450-563** — 인용: "지금까지의 내용을 Trace Generation 관점에서 정리합니다." → 문제: ‘Trace 생성 과정’, ‘Trace Generation의 결과물’, ‘Trace Replay’, ‘핵심 정리’가 guard→실행→resume을 반복합니다. 새로운 정보인 cache 한도와 dynamic shapes가 반복 설명 사이에 묻힙니다. → 제안: 첫 호출/재호출 비교 한 개와 cache 흐름도 한 개로 합치고, graph break의 예외와 dynamic shapes만 별도 설명으로 남깁니다.
- **[A2-5] [severity: medium] line 583** — 인용: "joint graph 생성과 forward/backward 분리(partitioning)" → 문제: AOTAutograd를 이번 강의 범위에서 뺐지만 joint graph, functionalization, decomposition을 설명 없이 동시에 도입합니다. 679행의 `boxed`도 호출 규약을 설명하지 않습니다. → 제안: “joint graph는 forward와 backward를 함께 담은 그래프”, “functionalization은 in-place 변경 등을 명시적인 입력·출력으로 바꾸는 변환”, “decomposition은 연산 분해”로 짧게 정의하고, boxed에는 “여러 위치 인자를 담은 리스트 하나로 받는 호출 규약”을 덧붙입니다.
- **[A2-6] [severity: medium] line 727-729** — 인용: "TorchDynamo의 상당히 많은 부분이 Python으로 작성되어 있습니다." → 문제: 질문은 Python에서만 사용할 수 있는지인데, 답은 구현 언어를 말합니다. Python 실행 의존성과 다른 언어에서 compiled artifact를 사용하는 문제가 구분되지 않습니다. → 제안: “Dynamo의 graph capture는 CPython 실행에 통합되어 있습니다. Python 밖으로 모델을 배포하는 경로는 `torch.export`와 별도의 컴파일·런타임 도구를 구분해서 살펴봐야 합니다.”로 답합니다.

## Axis 3 — 모든 도식 검토

Mermaid는 소스로 판단했고 `mindmap`은 없습니다. ThemeImage는 light/dark 양쪽을 열었습니다. 현재 CSS의 `.article-body img { height: auto; }` 때문에 `h-32` 같은 클래스만으로 실제 표시 높이를 단정하지 않았습니다. 브라우저 실행이 환경 제한으로 실패하여, 가독성 평가는 이미지 원본·SVG 라벨 크기·CSS 제약을 기준으로 했습니다.

- 적합 — line 69, `/images/03/slide03_1.png`: Dynamo→AOTAutograd→compiler/codegen backend 구분을 지원합니다. 2.0 당시 구조라는 캡션을 유지하면 좋습니다.
- **[A3-1] [severity: medium] line 112-132** — 인용: "이 예제에서는 코드 한 줄이 하나의 노드에 해당하지만" → 문제: CFG의 `L1-2` 노드는 이미 두 줄을 묶었고 `J1`은 원문 코드에 없는 합류점입니다. 세 조건을 하나의 `d = ?`로 합친 것도 소스 그대로의 basic-block CFG가 아니라 단순화한 그림입니다. → 제안: “조건 검사를 하나로 합친 개념도입니다. `L1-2`는 연속된 두 문장, `J1`은 분기 합류점을 나타냅니다.”로 설명합니다. 노드 수와 배치는 적절합니다.
- 적합 — line 141-144, Trace Mermaid: `d = 0` 경로를 앞의 단순화한 CFG와 같은 순서로 보여주며, 5개 노드로 읽기 쉽습니다.
- **[A3-2] [severity: medium] line 150-155** — 인용: "alt="Trace diagram"" → 문제: 두 trace는 색만 다르고 칸 수와 내용이 같아, 입력에 따라 경로가 어떻게 달라지는지 보여주지 못합니다. dark 버전의 `Program`과 실행 상자 글자 대비도 낮습니다. → 제안: 입력을 `d > 0`, `d = 0`으로 명시하고 칸에 `L1-2 → D → L4/L6 → J1 → L9`를 적어 차이를 드러냅니다. dark 라벨은 밝게 조정합니다.
- **[A3-3] [severity: medium] line 196-201** — 인용: "alt="Trace Cache diagram"" → 문제: 반복되는 구간 세 개만 표시하여, 주변에서 설명하는 비연속 블록의 저장·조회·decode 생략을 보여주지 않습니다. dark 버전에서는 회색 칸 경계가 특히 흐립니다. → 제안: 이 그림을 생략하고 217행의 A→E 비교 그림으로 설명을 통합하거나, 원본 주소 순서와 cache 저장 순서를 함께 표시합니다.
- 적합 — line 217, `/images/03/slide10_2.png`: 비연속 A→B→C→D→E 경로를 연속 저장하는 차이가 명확하고 라벨도 일치합니다. Cache miss 확률까지 증명하는 그림은 아닙니다.
- 적합 — line 224, `/images/03/slide10_1.png`: 인용 논문의 predictor/cache/buffer와 피드백 연결을 직접 보여줍니다. 본문의 Pentium 4 적용 범위와 buffer 역할은 A4-7·A4-8에서 별도로 수정해야 합니다.
- 적합 — line 244, `/images/03/slide11_1.png`: 프로그램+static input으로 특화 프로그램을 만들고 dynamic input으로 실행하는 두 단계를 정확히 구분합니다.
- **[A3-4] [severity: medium] line 261-270** — 인용: "alt="Partial Evaluation diagram"" → 문제: SVG의 `dynamic token`은 여기서 연산을 가리키는 비표준 명칭이며, `① 재계산 불필요` 지시선은 제거되는 주황색 칸이 아니라 남는 마지막 E 쪽을 가리킵니다. 본문의 ‘원래 Trace’ 목록에는 그림에 있는 주황색 계산이 빠져 있습니다. → 제안: 표준 단계명은 없으므로 `dynamic input에 의존하는 연산`으로 풀어 쓰고, 지시선을 주황색 칸에 연결합니다. 원본 목록에는 static 계산도 표시하거나 250행의 `(5 + 10) * y → 15 * y` 그림 하나로 대체합니다.
- 적합 — line 365, `/images/03/slide14_1.png`: source→compiler→bytecode→VM 실행을 지원하며 단순한 구조입니다. `Library modules`는 실행 중 사용하는 모듈이라는 한 줄이면 충분합니다.
- **[A3-5] [severity: medium] line 386** — 인용: "Call stack이 frame object의 linked list로 표현되는 Python VM 구조 다이어그램" → 문제: `PyFrameObject`와 `f_back` 그림은 과거 CPython 구조를 이해하는 데 적합하지만, 현대 CPython의 매 호출마다 이 객체가 만들어진다고 읽히면 부정확합니다. CPython 3.11부터 Python에 노출되는 frame object는 필요할 때 생성됩니다(확신: high). → 제안: “과거 CPython의 frame 객체 개념도”라고 표시하고, 현재의 실행 frame과 Python에 노출되는 `PyFrameObject`를 구분합니다. [CPython 3.11 변경 사항](https://docs.python.org/3.11/whatsnew/3.11.html#cheaper-lazy-python-frames)
- **[A3-6] [severity: medium] line 394-401** — 인용: "alt="TorchDynamo diagram"" → 문제: 구간 분할과 guard 위치는 본문에 맞지만 SVG 범례 `Graph break (동적 제어 흐름)`은 graph break를 한 원인과 동일시합니다. 지원하지 않는 호출 등도 원인이며, dark 버전의 설명·trace 번호 대비가 낮습니다. → 제안: 범례를 `Graph break 지점(예: tensor 값에 따른 Python 분기)`으로 바꾸고, 구간을 `captured graph + 잔여 Python 코드`로 이해할 수 있게 설명합니다. dark 라벨도 밝게 조정합니다.
- 적합 — line 414, `/images/03/slide21_1.png`: 기본 frame evaluation과 Dynamo의 guard·변환 bytecode·compiled function 호출 차이를 지원합니다. `PyFrameObject` 표기는 A3-5의 역사적 개념도 단서를 함께 적용합니다.
- **[A3-7] [severity: high] line 442-446** — 인용: "Bytecode analysis → bytecode transformation → FxGraph construction → user-defined compilation → guard insertion" → 문제: PNG는 작은 bytecode 목록 세 개와 여러 코드를 한 장에 넣어 발표 크기에서 읽기 어렵고, 본문의 직렬 5단계와 달리 분석에서 graph·guard·변환 코드가 파생되는 관계를 보여줍니다. `user-defined compilation` 단계도 이 PNG에는 명시되어 있지 않습니다. → 제안: graph break 전후 코드와 결과 graph를 두 장으로 나누고 “bytecode 분석 중 FX graph와 guard 조건을 수집하고, backend 결과를 호출하도록 bytecode를 변환한다”로 설명합니다. 그림의 `0.5` 예제도 A4-4와 함께 수정합니다.
- **[A3-8] [severity: high] line 467-492** — 인용: "TB -->|Select| RF1" → 문제: transformed bytecode에서 graph 계산과 resume 선택이 별개 가지로 뻗어, graph 결과를 받아 Python 분기를 실행한 뒤 resume한다는 순서가 사라집니다. 11개 노드와 긴 의사 함수명이 기본 최대 폭 60%에 들어가므로 축소 시 읽기 어렵습니다. → 제안: 실행 경로를 `guard → transformed bytecode → compiled callable → Python 분기/미지원 호출 → resumed function → 새 frame의 guard`로 연결하고, cache miss·재컴파일 한도는 별도 도식으로 분리합니다.
- **[A3-9] [severity: high] line 510-515** — 인용: "lightSrc="/images/03/trace-generation-light.svg"" → 문제: 새 frame으로 돌아가는 점선이 `Cache Entry Guard` 대신 transformed bytecode 상단에 연결되어 guard 검사를 건너뛰는 것으로 읽힙니다. 전체를 `PyFrameObject`로 감싸 cache와 graph가 호출별 frame 안에 저장되는 것처럼 보이며, dark 버전의 guard·graph break 글자도 흐립니다. → 제안: 반복 화살표를 guard 진입으로 연결하고, code object에 연결된 cache와 호출별 frame을 분리합니다. `Captured FxGraph`/`FxGraph 계산`은 실행 단계에서 `compiled callable`/`compiled callable 호출`로 표시하고 dark 대비를 높입니다. [2.13 cache 저장 구조](https://raw.githubusercontent.com/pytorch/pytorch/v2.13.0/torch/_dynamo/cache_size.py)
- **[A3-10] [severity: medium] line 570-591** — 인용: "I -->|Inductor Loop-Level IR| T["Triton"]" → 문제: 이 그림의 Triton이 Inductor의 codegen target인지 독립적인 Triton compiler인지 구분되지 않아, 내부 IR을 직접 입력받는 것으로 읽힐 수 있습니다. Inductor의 Triton codegen이 내부 IR에서 Triton 소스 코드를 생성하는 단계가 빠졌습니다(확신: high). → 제안: `Inductor(Loop-level IR → scheduling/fusion → Triton codegen) → Triton 소스 → Triton compiler → GPU kernel`로 바꾸고 GPU 경로임을 명시합니다. 4개 주 노드와 `wide` 사용 자체는 적절합니다. [2.13 TritonKernel.codegen_kernel](https://raw.githubusercontent.com/pytorch/pytorch/v2.13.0/torch/_inductor/codegen/triton.py)

## Axis 4 — 기술적 정확성

- **[A4-1] [severity: high] line 305-309** — 인용: "실제 Op을 수행하는 대신 trace만 collect하는 kernel을 붙여놓은 것이 `jit.trace`입니다." → 문제: `jit.trace`는 example inputs로 실제 함수를 실행하면서 tensor 연산을 기록합니다. 실연산을 생략하는 설명은 FakeTensor 기반 capture와 혼동을 만듭니다(확신: high). → 제안: “Tracing용 처리에서 graph node를 기록하고 실제 연산도 실행하여 출력을 얻습니다. Python 제어 흐름 자체는 graph에 기록하지 않습니다.”로 수정하고, CPU tensor 연산도 기록 대상임을 분명히 합니다. [공식 trace 동작 설명](https://docs.pytorch.org/docs/2.9/generated/torch.jit.trace.html)
- **[A4-2] [severity: high] line 300-313** — 인용: "Python subset 전용 Interpreter" → 문제: `jit.script`의 graph 생성은 Python subset을 실행해 trace하는 과정이 아니라 소스를 분석해 TorchScript로 컴파일하는 과정입니다. 만들어진 결과를 실행하는 TorchScript interpreter와 frontend compiler를 혼동했습니다(확신: high). → 제안: 제목과 표를 “Python subset을 TorchScript IR로 컴파일”로 바꾸고 “지원하는 `if`와 loop를 제어 흐름으로 보존한다”를 덧붙입니다. [공식 script 설명](https://docs.pytorch.org/docs/2.9/generated/torch.jit.script.html)
- **[A4-3] [severity: high] line 341** — 인용: "Trace를 뽑아주는 역할만 하고, 최적화는 별도입니다." → 문제: `jit.script`는 tracing이 아니며, `trace`/`script` 결과는 TorchScript의 JIT 최적화·실행 경로에 연결됩니다. Dynamo와 backend의 역할을 구분하려다가 TorchScript 전체에 최적화가 없는 것처럼 답했습니다(확신: high). → 제안: “TorchScript에도 최적화 경로가 있습니다. Dynamo는 주로 graph capture를 담당하고, `torch.compile`의 fusion·codegen은 Inductor 같은 backend가 수행합니다. 성능 차이는 실제 생성 graph와 kernel로 확인해야 합니다.”로 교체합니다. [TorchScript API 설명](https://docs.pytorch.org/docs/2.9/jit.html)
- **[A4-4] [severity: high] line 323-335** — 인용: "if x.mean() > 0.5:" → 문제: 앞줄이 `x = x.cos().cos()`이므로 일반적인 유한 실수·비어 있지 않은 입력에서 각 원소는 `[cos(1), 1]`, 즉 약 `[0.5403, 1]`에 들어갑니다. 평균도 0.5보다 커서 서로 다른 입력에 따른 분기 변경을 재현하지 못합니다(확신: high; Python `math`로 경계값 확인). → 제안: 임계값을 `0.8`로 바꾸고 원래 입력 `zeros`와 `full(..., π/2)`를 비교합니다. 수정 후 한 입력으로 trace하고 다른 입력의 eager 결과와 비교하는 검증까지 예제에 포함합니다.
- **[A4-5] [severity: high] line 536** — 인용: "프로세스 전체의 누적 재컴파일 횟수 상한입니다." → 문제: 2.13의 `accumulated_recompile_limit`은 프로세스 전체가 아니라 **code object**에 대한 제한입니다. `recompile_limit`도 단순한 ‘frame당 총 entry 수’가 아니라 같은 `ID_MATCH` 객체와 compilation region을 고려합니다(확신: high; 기본값 8·256과 alias 자체는 맞음). → 제안: “두 제한은 code object의 cache 증가와 재컴파일을 제어합니다. 기본 한도는 8이며 객체별 구분을 고려하고, 누적 한도 256은 같은 code object 전체에 적용됩니다.”로 고칩니다. 504행도 “중첩 호출의 신규 컴파일은 중단하지만 기존 cache는 재사용할 수 있다”로 보완합니다. [2.13 cache_size.py](https://raw.githubusercontent.com/pytorch/pytorch/v2.13.0/torch/_dynamo/cache_size.py), [재컴파일 시 중첩 cache 재사용](https://docs.pytorch.org/docs/main/user_guide/torch_compiler/compile/programming_model.recompilation.html#changing-the-cache-size-limit)
- **[A4-6] [severity: high] line 268** — 인용: "static input이 포함된 연산을 모두 제거하고 dynamic input만 남긴 형태로 최적화할 수 있습니다." → 문제: static input이 **포함된** 연산과 static input에**만 의존하는** 연산은 다릅니다. 바로 앞의 `15 * y`처럼 상수와 동적 값을 함께 쓰는 연산은 남아야 합니다(확신: high). → 제안: “Static input에만 의존하는 계산은 미리 평가하고, 그 결과를 상수로 반영한 나머지 계산을 남깁니다.”로 교체합니다.
- **[A4-7] [severity: high] line 190-207** — 인용: "Trace miss가 발생하면 trace에서 빠져나와 instruction cache에서 하나씩 fetch" → 문제: Pentium 4라는 제목 아래 일반 trace-cache 연구 구조와 NetBurst 구현을 섞었습니다. Pentium 4의 trace cache miss는 L2에서 명령어를 가져와 decode하는 경로이며, ‘별도 L1 instruction cache에서 하나씩’이라는 설명이 아닙니다(확신: high). → 제안: 제목을 “Trace cache의 개념과 연구 구조”로 바꾸고 인용 논문을 설명하거나, Pentium 4 절을 분리하여 `L2 → fetch/decode → trace cache` 경로로 설명합니다. [Intel의 Pentium 4 설명](https://www.intel.com/content/dam/www/public/us/en/documents/research/2001-vol05-iss-1-intel-technology-journal.pdf)
- **[A4-8] [severity: high] line 213-229** — 인용: "성능 향상 효과가 클 것 같은 후보를 먼저 모아두고 선별하는 버퍼" → 문제: 인용 논문의 outstanding trace buffers는 새 trace를 구성하고, 실행 중 branch 결과를 추적하여 잘못 예측한 trace를 복구합니다. Cache hit trace도 buffer로 전달되므로 단순한 ‘유망 후보 선별함’이 아닙니다(확신: high). → 제안: “Trace 구성과 branch 결과 추적·오예측 복구를 위한 버퍼”로 정의하고, 동작 목록에 hit trace 전달과 misprediction repair를 추가합니다. [원논문 §2.2](https://ericrotenberg.wordpress.ncsu.edu/files/2025/06/paper_IEEE-TC-Feb-1999.pdf)
- **[A4-9] [severity: medium] line 192** — 인용: "Bank conflict가 발생하거나 cache miss로 원하는 데이터가 캐시에 없을 가능성이 높아지기 때문입니다." → 문제: 코드 주소가 비연속이라는 사실만으로 cache miss나 bank conflict 확률이 높아진다고 일반화할 수 없습니다. Trace cache의 동기는 cache에 명령어가 있어도 여러 taken branch를 넘는 fetch 대역폭 확보가 어렵다는 데 있습니다(확신: high). → 제안: “여러 taken branch를 넘어서 비연속 명령어를 같은 cycle에 공급하려면 복잡한 fetch 구조가 필요합니다.”로 바꾸고 219행의 miss 확률 주장도 삭제합니다. [원논문 abstract·§1](https://ericrotenberg.wordpress.ncsu.edu/files/2025/06/paper_IEEE-TC-Feb-1999.pdf)
- **[A4-10] [severity: medium] line 353** — 인용: "Python 전체 언어를 지원 (`jit.script`의 subset 문제 해결)" → 문제: 모든 Python 구문을 graph로 capture할 수 있다는 의미로 읽힙니다. Dynamo에는 지원 범위가 있고 `fullgraph=False`에서 graph break·skip·eager 실행으로 기존 Python 코드와 함께 동작합니다(확신: high). → 제안: “TorchScript subset으로 코드를 다시 작성할 필요를 줄이고, capture 가능한 구간을 컴파일하며 나머지는 Python 실행과 연결합니다.”로 고칩니다. 301행의 장점도 같은 표현으로 맞춥니다. [공식 skipped functions 설명](https://docs.pytorch.org/docs/main/user_guide/torch_compiler/compile/programming_model.skipped_functions.html)
- **[A4-11] [severity: high] line 380** — 인용: "frame 내부에 code object와 evaluation loop이 존재" → 문제: Frame은 실행 상태를 담는 자료구조이며 evaluation loop를 소유하지 않습니다. Evaluation loop가 frame의 명령 위치·지역 변수·평가 stack 등을 사용해 코드를 실행합니다(확신: high). → 제안: “Frame은 code object 참조, 지역 변수, 평가 stack, 실행 위치 등을 보관합니다. Evaluation loop가 이 상태를 읽고 갱신합니다.”로 교체합니다. [CPython frame 실행 설명](https://docs.python.org/3.11/whatsnew/3.11.html#inlined-python-function-calls)
- **[A4-12] [severity: medium] line 548** — 인용: "기록이나 최적화 없이 호출될 때마다 같은 방식으로 반복" → 문제: CPython 3.11 이후에는 adaptive specialization과 inline cache가 있어, Dynamo가 없다고 runtime 최적화가 전혀 없는 것은 아닙니다(확신: high). → 제안: “CPython이 bytecode를 해석·실행하며, Dynamo의 tensor graph capture와 backend compilation은 수행하지 않습니다.”로 한정합니다. [CPython PEP 659 적용](https://docs.python.org/3.11/whatsnew/3.11.html#pep-659-specializing-adaptive-interpreter)
- **[A4-13] [severity: medium] line 538** — 인용: "`dynamic=True`/`dynamic=False`로 이 자동 전환을 강제 On/Off할 수 있습니다." → 문제: `True`는 shape 변경 후 자동 전환을 켜는 것이 아니라 **첫 컴파일부터** 가능한 dynamic shape 처리를 시도합니다. 454·460행의 모든 Python 값·메타데이터가 ‘동일’해야 한다는 설명도 symbolic 값과 범위 조건을 누락합니다(확신: high). → 제안: “`None`: 필요할 때 dynamic shape으로 재컴파일, `True`: 처음부터 가능한 범위를 dynamic하게 처리, `False`: shape에 specialize”로 구분하고 guard는 “캡처 때 세운 전제가 유효한지 검사”라고 정의합니다. [2.13 기본 shape 설정](https://raw.githubusercontent.com/pytorch/pytorch/v2.13.0/torch/_dynamo/config.py), [dynamic 인자 의미](https://docs.pytorch.org/docs/2.14/generated/torch.compile.html)
- **[A4-14] [severity: high] line 657-670** — 인용: "forward_output = model(input)" → 문제: 앞 예제의 입력 변수는 `my_input`인데 여기서는 Python 내장 함수 `input`을 전달합니다. `MyRuntime`·`set_model`·`my_compiler`도 구현되지 않아 그대로 실행 가능한 예제가 아닙니다(확신: high; 정적 검토). → 제안: 실행 예제는 `def my_backend(gm, example_inputs): return gm.forward`, `model = torch.compile(MyModule(), backend=my_backend)`, `forward_output = model(my_input)`으로 만듭니다. 외부 compiler/runtime 연결 예제는 별도로 ‘의사코드’라고 표시하고 반환 객체의 `__call__` 계약을 설명합니다. [최소 custom backend 예제](https://docs.pytorch.org/docs/2.14/user_guide/torch_compiler/torch.compiler_custom_backends.html#overview)
- **[A4-15] [severity: medium] line 677-688** — 인용: "AOTAutograd는 Core Aten opset으로 구성된 FX graph를 만들어내기 때문입니다." → 문제: 공식 custom backend 문서에도 같은 요약이 있지만, 이것을 ‘래퍼만 씌우면 Core ATen만 남는다’는 보장으로 사용하기에는 검증이 필요합니다. 2.13 소스는 전달된 decomposition 설정을 사용하며 기본 `aot_autograd_decompositions`는 빈 dict입니다(확신: medium; **needs verification**). → 제안: “정확한 출력 opset은 decomposition 설정에 따라 확인해야 합니다.”를 추가하고, 2.13 환경에서 Core ATen 밖의 입력 op를 포함한 graph를 `fw_compiler`에서 출력해 확인합니다. 필요한 decomposition table을 명시한 뒤 지원 범위를 보장합니다. [2.13 AOTAutograd 소스](https://raw.githubusercontent.com/pytorch/pytorch/v2.13.0/torch/_functorch/aot_autograd.py), [공식 문서의 요약](https://docs.pytorch.org/docs/2.14/user_guide/torch_compiler/torch.compiler_custom_backends.html#custom-backends-after-aotautograd)
- **[A4-16] [severity: medium] line 619** — 인용: "https://docs.pytorch.org/docs/stable/user_guide/torch_compiler/torch.compiler_ir.html" → 문제: 599·699행은 2.13의 정확한 개수 193·123을 제시하지만 출처는 이동하는 `stable`입니다. 검토 시 이 URL은 2.14로 연결되었고 2.13 문서는 접근되지 않아, 수치의 버전별 일치는 **needs verification**입니다(확신: high: 링크 비고정, low: 2.13 개수 미검증). → 제안: 검증 가능한 2.13 문서 스냅샷 또는 소스 태그와 집계 기준(operator packet인지 overload인지)을 명시하고, 확인 전에는 “약 190개”, “약 120개”만 남깁니다. 416행도 같은 방식으로 버전을 고정합니다. [현재 stable IR 문서](https://docs.pytorch.org/docs/stable/user_guide/torch_compiler/torch.compiler_ir.html)
- **[A4-17] [severity: medium] line 651** — 인용: "두 번째 실행부터는 cache된 컴파일 결과를 재활용하여 compilation overhead 없이 최적화된 코드가 실행됩니다." → 문제: 새 shape·Python 값·미방문 분기 등의 조건에 따라 두 번째 이후 호출에서도 컴파일할 수 있습니다. 뒤의 717행에는 이미 ‘recompilation이 없는 한’이라는 정확한 단서가 있습니다(확신: high). → 제안: “이후 호출에서는 guard를 통과하는 cache 결과를 재사용합니다. 새로운 조건이나 아직 컴파일되지 않은 구간에서는 추가 컴파일이 발생할 수 있습니다.”로 통일합니다. [torch.compile cache 설명](https://docs.pytorch.org/docs/2.14/generated/torch.compile.html)

- **[A4-18] [severity: medium] line 174** — 인용: "일반적인 프로그램에서 매 5～6개 instruction마다 branch가 발생한다는 것입니다." → 문제: Workload와 측정 근거가 없어 보편적인 수치로 확인할 수 없습니다(확신: low; **needs verification**). 이어지는 설명도 branch prediction이 없는 경우의 제한임을 첫 문장에서 밝혀야 합니다. → 제안: 측정 대상·출처를 붙이거나 “분기가 잦은 코드에서는 branch prediction 없이 다음 경로의 instruction을 공급하기 어렵습니다.”로 바꿉니다.

검증 범위: 1 GHz = 초당 10억 cycle, 250행의 상수 접기, `Linear(100, 10)`에 `[10, 100]`을 넣었을 때의 출력 shape `[10, 10]`은 일관됩니다. `aot_autograd`, `make_boxed_func`, `register_backend`, `list_backends`는 실재 API이며, TorchScript의 2.10 deprecation 서술은 [공식 릴리스 설명](https://pytorch.org/blog/pytorch-2-12-release-blog/)과 일치합니다. 이 파일에는 distributed collectives·DTensor/FSDP·CUDA stream·vLLM의 구체적 동작 주장이나 bandwidth/FLOP·byte-count 계산은 없습니다.

</details>
